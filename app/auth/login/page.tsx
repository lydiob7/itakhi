import LoginPage from "@/app/components/auth/LoginPage";
import generalMetadata from "@/config/metadata";

export const metadata = generalMetadata("Login - Itakhi Digital");

const page = () => {
    return <LoginPage />;
};

export default page;

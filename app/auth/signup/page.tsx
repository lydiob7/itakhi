import SignupPage from "@/app/components/auth/SignupPage";
import generalMetadata from "@/config/metadata";

export const metadata = generalMetadata("Signup - Itakhi Digital");

const page = () => {
    return <SignupPage />;
};

export default page;
